{
  "name": "tasksplitting",
  "private": true,
  "workspaces": ["client"],
  "scripts": {
    "dev": "concurrently \"mvn -f server/pom.xml spring-boot:run\" \"npm run dev --workspace client\"",
    "build": "mvn -f server/pom.xml package -DskipTests && npm run build --workspace client",
    "typecheck": "npm run typecheck --workspace client"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
