# 前端 API Base URL（可选）
#
# 留空（默认）：所有请求使用相对路径（如 /api/auth/login），由 Vite dev-server
#               通过 proxy 转发到 http://localhost:4000（见 vite.config.ts）。
# 设置一个完整的 origin：请求将直接发往该 origin，例如
#
#   VITE_API_BASE_URL=https://api.example.com
#
# 复制本文件为 .env.local 或 .env.development 后即可在 dev 环境生效；
# 生产构建时也可由 CI/部署环境通过 -e 注入。

VITE_API_BASE_URL=
