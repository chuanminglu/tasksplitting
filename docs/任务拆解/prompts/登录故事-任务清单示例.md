# 登录故事 · 任务拆分示例（v3：新增证据分级标注 [FACT]/[INFER]/[ASSUME]/[UNKNOWN]）

> 由最新版 `story拆分-subagent提示词.md` 针对以下故事实际生成（通过 `Agent` 工具真实调用一次 `general-purpose` subagent产出，非手工编造）：
> "作为一名系统用户，我希望能够使用用户名和密码登录系统，登录成功后进入我的工作台，以便安全地访问只属于我的功能和数据。目前系统还没有任何登录/鉴权机制。代码库技术栈：Node.js + Express + Prisma（后端），React + TS（前端）。"
>
> 输入材料：
> - 材料2（REPO_PATH）：未提供，本次刻意测试"无代码库自查"路径的表现
> - 材料3（验收标准）：AC-1登录成功跳转工作台 / AC-2账号不存在与密码错误分别提示 / AC-3连续5次密码错误锁定15分钟 / AC-4记住我7天否则2小时会话 / AC-5登录成功失败埋点
>
> 与上一版示例的差异（这一版新加的能力）：
> 1. 证据分级标注对齐 `.github/copilot-instructions-v2.md`：关键判断、每份任务的"预计涉及文件"、DoD里的数值统一打 `[FACT]`（材料/自查明确给出）/`[INFER]`（推理得出）/`[ASSUME]`（合理假设，材料未覆盖）/`[UNKNOWN]`（无法判断需确认）四档标签
> 2. "假设与待确认项"成为全篇 `[ASSUME]`/`[UNKNOWN]` 的汇总入口，能看到本次生成一共留了几处不确定、分别是什么
> 3. 后续任务（任务2~6）里基于前序任务"预计涉及文件"类推出来的路径，标了 `[INFER]` 并注明"需要用前序任务实际交付结果核对"，不是和自查结果一样可信的 `[FACT]`
>
> 之前版本已验证的能力（这次继续保留，不重复说明）：交付物统一叫"任务"不用"简报"；去掉了"被派来的agent"拟人化措辞；任务数量不受"3-7个"限制，本次仍是6个；末尾有"任务执行方式说明"。

---

NEEDS CLARIFICATION 未触发（未提供 REPO_PATH 属于"跳过自查"分支，不属于"查完仍无法确定"分支），按方法论正常出方案，缺口已收入第6节假设清单。

## 1. 调查过程

未提供代码库路径（REPO_PATH 为空），跳过 Grep/Glob/git log 自查。判断依据完全来自材料1原文："目前系统还没有任何登录/鉴权机制"——`[FACT]`（材料明确陈述，非推断）。

## 2. 模式判断

**Walking Skeleton**（走通骨架法：先搭一条最薄的端到端路径跑通，再逐片加厚功能，全程用特性开关包裹到验收通过再对外开放）。

依据：`[FACT]` 材料1原文明确"系统还没有任何登录/鉴权机制"，即代码库中不存在需要保留兼容、不能说停就停的旧登录实现——不满足 Expand-Contract（新旧并存迁移法：新旧实现并行、调用方逐个切换、确认无依赖后删旧代码）的适用前提（旧实现的存在）。因此本故事是纯新增能力，选 Walking Skeleton，而非 Expand-Contract。

## 3. 任务序列（路线图）

| # | 任务内容 | 前后端分工 | DoD（摘要） | Flag包裹 |
|---|---------|-----------|------------|---------|
| 1 | 端到端骨架：最小登录成功路径（AC-1 happy path） | 后端：User模型+登录API+密码校验；前端：登录表单+调用API+跳转工作台 | 正确账号密码 → 200 + 跳转工作台，集成测试覆盖 | 是 |
| 2 | 失败路径分支：账号不存在 / 密码错误（AC-2） | 后端：区分两种失败原因返回不同错误码；前端：按错误码展示对应文案 | 两种失败场景分别断言对应提示文案 | 是 |
| 3 | 业务规则：连续失败锁定（AC-3） | 后端：失败计数字段+锁定时间字段+校验锁定态；前端：锁定态提示文案 | 连续5次错误后第6次（即使密码正确）被拒绝，15分钟后恢复 | 是 |
| 4 | 业务规则：记住我/会话时长（AC-4） | 后端：按记住我标志签发不同过期时间的会话凭证；前端：记住我勾选框+透传参数 | 勾选→凭证有效期≈7天；不勾选→≈2小时 | 是 |
| 5 | 非核心工作：登录成功/失败埋点（AC-5） | 后端：在成功/两种失败/锁定四个分支各自触发埋点事件；前端：无需改动（或补充埋点SDK初始化） | 四种结果分支均产生对应埋点事件，字段可被运营看板消费 | 是 |
| 6 | 发布收尾：工作台访问保护全量开启 + Flag清理 | 后端：工作台路由强制要求登录态中间件常态生效；前端：未登录访问工作台重定向登录页；双端删除flag判断分支 | 无flag情况下，AC-1~AC-5行为与flag开启时一致；flag相关代码/配置项已删除，仓库中不再存在该开关引用 | 否（本任务即删除flag） |

切分技巧标注：任务1为"按工作流步骤"（先跑通最短主干路径）；任务2为"按happy-unhappy path"；任务3、4为"按业务规则变化"；任务5为"延迟非核心工作"（埋点不阻塞主流程验证，单独成片）；任务6为flag生命周期收口，不属于常规切分技巧，而是 Walking Skeleton 流程本身要求的"退出骨架状态"步骤。

## 4. 特性开关设计

- **类型**：Release Toggle（发布开关：短命，用于把"合并到主干"和"对用户可见"解耦，全量发布后必须删除，不允许长期存在，避免开关债务）
- **命名建议**：`feature_login_auth_enabled`（`[ASSUME]` 具体命名规范材料未给出，仅按业界惯例给出参考值）
- **翻转条件**：任务1~5合并后，在预发/灰度环境开启验证AC-1~AC-5全部通过，且AC-5埋点数据在运营看板可见、口径正确后，全量打开；打开后进入任务6，把flag判断从代码中物理删除（含配置项、双端if分支），而不是仅仅让默认值保持on——删除本身就是任务6的交付物。

## 5. 反模式自检

- 横向按前端/后端/测试拆顶层任务 → 未触犯，每个任务都是前后端联动的端到端纵向切片
- 把Mock当独立任务立项 → 未触犯，任务序列中无独立Mock任务
- 测试放最后阶段 → 未触犯，每个任务的DoD都内嵌对应测试点，不集中到最后
- 对已上线行为做一次性切换 → 未触犯，任务6对"工作台需要登录"这一行为变更仍走flag渐进开启，而非直接硬切
- 对全新功能强套Expand-Contract → 未触犯，已判定为全新能力并选择Walking Skeleton
- 用"故事已经很小"代替PR小批量纪律 → 未触犯，仍按6个可独立合并的PR拆分，未以"故事简单"为由合并任务
- 批准的PR攒着不合并 → 未触犯，见第8节执行方式说明，强调逐个合并、逐个派发
- Flag忘记清理 → 未触犯，任务6专门承担flag删除职责，且明确写入DoD

## 6. 假设与待确认项

- `[ASSUME]` 未提供REPO_PATH，跳过自查，本方案所有"预计涉及文件"均基于材料1给出的技术栈（Node.js+Express+Prisma / React+TS）按通用工程惯例推测，不代表真实仓库结构
- `[ASSUME]` 密码加密算法采用 bcrypt（材料未指定具体加密方式）
- `[ASSUME]` "工作台"页面在本故事启动前可能已作为无保护页面存在，本故事新增的核心行为是"访问工作台需要登录态"这一约束本身，而非从零搭建工作台业务内容
- `[ASSUME]` 用户账号数据的创建/注册不在本故事范围内，Task1假设已有可用于测试的用户记录（如手工种子数据），登录功能只做校验、不做注册
- `[UNKNOWN]` 用户账号从何处创建/初始化（是否存在独立的注册故事，或需要管理员后台手动建号）——材料未说明，需与产品/上游确认
- `[ASSUME]` 会话凭证的技术实现方式（JWT 或 服务端 session + cookie）未指定，留给实现方按现有基础设施选型；但AC-4给出的7天/2小时时长数值本身是材料原文明确值，标`[FACT]`
- `[ASSUME]` AC-5埋点的具体投递目标（日志表/现成埋点SDK/第三方分析平台）材料未指定，Task5的DoD只约束"四类结果分支各自产生一次可被运营看板消费的埋点事件"，具体落地技术选型留给实现方
- `[UNKNOWN]` 是否已有现成的Feature Flag基础设施（如LaunchDarkly、自建配置中心），还是需要新增一个最简单的环境变量/配置项作为开关——因无代码库无法确认，Task1需要实现方在交付报告里说明选用方式
- `[UNKNOWN]` "5次连续错误"的计数是否需要跨设备/跨IP共享（如同一账号在两台设备各错2次是否累加到5次）——AC-3原文未细化，Task3需实现方按"账号维度全局计数"这一最直白解释实现，如产品另有要求需回头修正

## 7. 任务清单

## 任务1

你要做的事：搭建登录功能最薄的端到端骨架——用户输入正确的用户名密码后，能成功登录并跳转到工作台页面。
对应验收标准：AC-1（用户名密码正确 → 登录成功，跳转工作台）

本任务信息自包含：不要假设除下文之外还有更多规划背景，下面就是执行这个任务所需的全部上下文（不管你是被派去执行它的subagent、另一个新对话，还是人工阅读）。

### 背景
这是"用户名密码登录"纵向安全切片计划中的第1/6个任务。前置状态：代码库中此前不存在任何登录/鉴权机制（无User模型、无登录路由、无登录页面）。本任务完成后，整体功能仍由 `feature_login_auth_enabled` 特性开关包裹，对普通用户不可见（仅在开启该开关的环境下可访问）。

### 预计涉及文件
未提供代码库路径，无法预估文件，由实现方在交付报告中列出实际改动文件 `[UNKNOWN]`。以下为基于材料1技术栈（Node.js + Express + Prisma 后端，React + TS 前端）给出的参考路径，不能替代上面的强制回填要求：
- `prisma/schema.prisma`（新增 User 模型：id / username / passwordHash 等字段）`[ASSUME]`
- 对应的 Prisma migration 文件（新增）`[ASSUME]`
- 后端登录路由文件，如 `src/routes/auth.ts`（新增）`[ASSUME]`
- 后端登录逻辑文件，如 `src/services/authService.ts`（新增，含密码比对）`[ASSUME]`
- 特性开关读取逻辑，如 `src/config/featureFlags.ts`（新增）`[ASSUME]`
- 前端登录页面，如 `src/pages/Login.tsx`（新增）`[ASSUME]`
- 前端登录表单组件，如 `src/components/LoginForm.tsx`（新增）`[ASSUME]`
- 前端调用登录API的封装，如 `src/api/authApi.ts`（新增）`[ASSUME]`
- 前端路由配置文件，如 `src/App.tsx` 或 `src/routes/index.tsx`（修改，新增 `/login` 路由）`[ASSUME]`

### 你要做什么
- 后端：在 Prisma schema 中新增最小 User 模型（用户名、密码哈希，其余字段留空/默认值即可）；新增登录接口（如 `POST /api/auth/login`），接收用户名密码，用 bcrypt 校验密码（假设值，需在交付报告中注明实际选用方案），校验通过后返回登录凭证（token 或 session，具体形式由实现方选型并在交付报告中说明）；接口整体注册在特性开关判断之内，开关关闭时接口不可访问或直接返回404/开关状态提示。
- 前端：新增登录页面（用户名+密码输入框、提交按钮）；提交后调用登录接口，成功后跳转到工作台路由（工作台页面若不存在，可用一个占位页面代替，占位页面不属于本任务的功能范围，只作为跳转目标存在）。
- 本任务只覆盖"用户名密码均正确"这一条路径，不处理任何错误分支。

### 明确不要做的事
- 不要实现账号不存在/密码错误的错误提示（属于任务2）
- 不要实现连续失败锁定逻辑（属于任务3）
- 不要实现"记住我"与会话时长差异化（属于任务4）
- 不要实现登录埋点（属于任务5）
- 不要修改特性开关的开放范围/翻转逻辑（该判断由任务6统一处理）
- 不要在工作台页面之外做额外重构或补充业务内容

### 完成定义（DoD）
- `[FACT]` 对应AC-1：输入正确的用户名和密码，登录接口返回成功状态（如200），前端收到成功响应后跳转至工作台路由；该路径有对应的自动化测试覆盖（后端可用接口集成测试，前端可用组件/路由级测试）。
- 特性开关关闭时，登录入口对普通用户不可达；开关开启时上述路径可正常跑通（用手工或环境变量方式验证均可，交付报告需说明验证方式）。
- 至少存在一条可复现的测试用户记录，用于测试正确用户名密码场景（`[ASSUME]` 用户数据来源，如手工种子数据/测试fixture，需在交付报告中说明具体做法）。

### 交付要求
改动应能独立形成一个PR，范围只覆盖本任务。完成后返回：实际改了哪些文件（与"预计涉及文件"逐项核对，一致的不用重复说明，不一致的必须说明原因）、新增了哪些测试用例、是否有偏离本任务范围的地方。

---

## 任务2

你要做的事：为登录接口补充两种失败路径——账号不存在、密码错误——并分别给出明确、互不相同的提示。
对应验收标准：AC-2（账号不存在 / 密码错误 → 分别给出明确提示）

本任务信息自包含：不要假设除下文之外还有更多规划背景，下面就是执行这个任务所需的全部上下文（不管你是被派去执行它的subagent、另一个新对话，还是人工阅读）。

### 背景
这是"用户名密码登录"纵向安全切片计划中的第2/6个任务。前置状态：任务1已合并，端到端登录骨架已跑通（后端有登录接口和User模型，前端有登录页面），但目前只处理"用户名密码均正确"的成功路径，其余情况尚无区分处理。本任务完成后，整体功能仍由 `feature_login_auth_enabled` 特性开关包裹，对用户不可见。

### 预计涉及文件
未提供代码库路径，无法预估文件，由实现方在交付报告中列出实际改动文件 `[UNKNOWN]`。参考路径（基于任务1实际改动结果类推，任务1合并后如与此处不符，需在派发本任务前更新）：
- 任务1新增的后端登录逻辑文件，如 `src/services/authService.ts`（修改，新增两类失败分支判断）`[INFER]`（基于任务1预计新增文件类推）
- 任务1新增的后端登录路由文件，如 `src/routes/auth.ts`（修改，错误响应结构调整）`[INFER]`
- 任务1新增的前端登录表单组件，如 `src/components/LoginForm.tsx`（修改，展示不同错误文案）`[INFER]`
- 前端错误码/文案映射文件（新增，如 `src/constants/authErrors.ts`）`[ASSUME]`

### 你要做什么
- 后端：登录接口在密码比对前先判断用户名是否存在；不存在时返回一种错误标识（如 `USER_NOT_FOUND`），存在但密码不匹配时返回另一种错误标识（如 `INVALID_PASSWORD`），两者的错误标识/错误码必须可区分。
- 前端：根据接口返回的错误标识，分别展示对应的提示文案（如"账号不存在"“密码错误”），两类提示文案不能相同或含糊表述为统一的"用户名或密码错误"。
- 本任务不涉及失败次数计数或锁定逻辑。

### 明确不要做的事
- 不要实现连续失败次数计数与锁定（属于任务3）
- 不要实现"记住我"与会话时长差异化（属于任务4）
- 不要实现登录埋点（属于任务5）
- 不要修改特性开关的开放范围/翻转逻辑
- 不要在已有登录成功路径（任务1产出）之外做额外重构

### 完成定义（DoD）
- `[FACT]` 对应AC-2：使用不存在的用户名登录，收到与"密码错误"场景不同的、明确指出"账号不存在"的提示；使用存在的用户名+错误密码登录，收到明确指出"密码错误"的提示；两种场景各有对应测试用例断言返回的错误标识/文案不同。

### 交付要求
改动应能独立形成一个PR，范围只覆盖本任务。完成后返回：实际改了哪些文件（与"预计涉及文件"逐项核对，一致的不用重复说明，不一致的必须说明原因）、新增了哪些测试用例、是否有偏离本任务范围的地方。

---

## 任务3

你要做的事：实现连续密码错误达到5次后锁定账号15分钟的规则。
对应验收标准：AC-3（连续5次密码错误 → 锁定账号15分钟）

本任务信息自包含：不要假设除下文之外还有更多规划背景，下面就是执行这个任务所需的全部上下文（不管你是被派去执行它的subagent、另一个新对话，还是人工阅读）。

### 背景
这是"用户名密码登录"纵向安全切片计划中的第3/6个任务。前置状态：任务1、任务2已合并，登录接口已能区分"账号不存在"和"密码错误"两类失败，尚无失败次数记录与锁定机制。本任务完成后，整体功能仍由 `feature_login_auth_enabled` 特性开关包裹，对用户不可见。

### 预计涉及文件
未提供代码库路径，无法预估文件，由实现方在交付报告中列出实际改动文件 `[UNKNOWN]`。参考路径（需在派发本任务前，用任务2的实际交付结果核对是否仍准确）：
- `prisma/schema.prisma`（修改，User模型新增 `failedLoginCount` 与 `lockedUntil` 字段）`[ASSUME]`
- 对应新增的 Prisma migration 文件（新增）`[ASSUME]`
- 后端登录逻辑文件，如 `src/services/authService.ts`（修改，新增计数与锁定判断）`[INFER]`
- 前端登录表单/错误文案映射（修改，新增"账号已锁定"提示分支）`[INFER]`

### 你要做什么
- 后端：登录接口在密码校验前先检查账号是否处于锁定状态，若锁定未到期，直接拒绝并返回锁定提示（不再进行密码比对，避免消耗新的失败次数）；密码错误时失败计数+1，累计达到5次（连续，即中间无一次成功登录打断计数）时将账号锁定15分钟，并记录锁定截止时间；登录成功时清零失败计数。
- 前端：识别账号锁定的错误标识，展示明确的锁定提示（如"账号已锁定，请15分钟后重试"）。
- 锁定期满后（即当前时间超过锁定截止时间），账号应能像未锁定时一样正常校验密码，不需要人工解锁操作。

### 明确不要做的事
- 不要实现"记住我"与会话时长差异化（属于任务4）
- 不要实现登录埋点（属于任务5）
- 不要修改特性开关的开放范围/翻转逻辑
- 不要提前实现锁定期间的埋点上报（锁定事件的埋点在任务5统一处理）
- 不要做超出User模型锁定相关字段之外的schema重构

### 完成定义（DoD）
- `[FACT]` 对应AC-3：模拟连续5次密码错误后，第6次即便输入正确密码也被拒绝，且返回明确锁定提示；有测试断言"锁定截止时间"约为触发锁定时刻起15分钟（`[FACT]` 15分钟数值取自AC-3原文）。
- 有测试断言：锁定期满（可用时间mock方式模拟）后，正确密码可重新登录成功，且失败计数被重置为0。
- 有测试断言：连续失败次数被一次成功登录打断后（如错2次后登录成功），失败计数归零，不会被后续错误直接累加到锁定阈值。

### 交付要求
改动应能独立形成一个PR，范围只覆盖本任务。完成后返回：实际改了哪些文件（与"预计涉及文件"逐项核对，一致的不用重复说明，不一致的必须说明原因）、新增了哪些测试用例、是否有偏离本任务范围的地方。

---

## 任务4

你要做的事：实现"记住我"勾选后会话保持7天、不勾选则2小时会话过期的差异化会话时长。
对应验收标准：AC-4（勾选"记住我" → 7天内免登录；不勾选 → 2小时会话）

本任务信息自包含：不要假设除下文之外还有更多规划背景，下面就是执行这个任务所需的全部上下文（不管你是被派去执行它的subagent、另一个新对话，还是人工阅读）。

### 背景
这是"用户名密码登录"纵向安全切片计划中的第4/6个任务。前置状态：任务1~3已合并，登录接口已能处理成功、账号不存在、密码错误、账号锁定四种情况，会话/登录凭证目前统一为固定有效期（或无差异化过期设置，取决于任务1的实际实现，需在派发本任务前核对）。本任务完成后，整体功能仍由 `feature_login_auth_enabled` 特性开关包裹，对用户不可见。

### 预计涉及文件
未提供代码库路径，无法预估文件，由实现方在交付报告中列出实际改动文件 `[UNKNOWN]`。参考路径（需用任务1实际的会话凭证实现方式核对，如实现为JWT则改签发逻辑，如为server session则改cookie maxAge）：
- 后端登录逻辑文件，如 `src/services/authService.ts`（修改，按记住我标志设置不同过期时间）`[INFER]`
- 后端登录路由文件，如 `src/routes/auth.ts`（修改，接收并透传记住我参数）`[INFER]`
- 前端登录表单组件（修改，新增"记住我"勾选框）`[INFER]`
- 前端调用登录API的封装（修改，透传记住我参数）`[INFER]`

### 你要做什么
- 后端：登录接口新增一个"记住我"布尔参数；勾选为true时签发的会话凭证有效期约为7天，为false（或未传）时有效期约为2小时。
- 前端：登录表单新增"记住我"勾选框，默认不勾选，勾选状态随登录请求一并提交。

### 明确不要做的事
- 不要实现登录埋点（属于任务5）
- 不要修改特性开关的开放范围/翻转逻辑
- 不要改动账号锁定相关逻辑（任务3产出）
- 不要新增除"记住我"外的其他登录表单字段

### 完成定义（DoD）
- `[FACT]` 对应AC-4：勾选"记住我"登录后，会话凭证的有效期断言约为7天（`[FACT]` 数值取自AC-4原文）；不勾选登录后，会话凭证有效期断言约为2小时（`[FACT]` 数值取自AC-4原文）；两种场景均有对应自动化测试验证过期时间戳/maxAge差异。

### 交付要求
改动应能独立形成一个PR，范围只覆盖本任务。完成后返回：实际改了哪些文件（与"预计涉及文件"逐项核对，一致的不用重复说明，不一致的必须说明原因）、新增了哪些测试用例、是否有偏离本任务范围的地方。

---

## 任务5

你要做的事：为登录成功、账号不存在、密码错误、账号锁定四类结果分支分别打点埋点，供运营看板统计。
对应验收标准：AC-5（登录成功/失败需要埋点，供运营看板统计）

本任务信息自包含：不要假设除下文之外还有更多规划背景，下面就是执行这个任务所需的全部上下文（不管你是被派去执行它的subagent、另一个新对话，还是人工阅读）。

### 背景
这是"用户名密码登录"纵向安全切片计划中的第5/6个任务。前置状态：任务1~4已合并，登录接口已完整覆盖成功、账号不存在、密码错误、账号锁定、记住我差异化会话等行为分支，但尚无任何埋点上报。本任务完成后，整体功能仍由 `feature_login_auth_enabled` 特性开关包裹，对用户不可见。

### 预计涉及文件
未提供代码库路径，无法预估文件，由实现方在交付报告中列出实际改动文件 `[UNKNOWN]`。参考路径：
- 后端登录逻辑文件，如 `src/services/authService.ts`（修改，在各分支出口调用埋点上报）`[INFER]`
- 埋点/日志上报封装文件，如 `src/lib/analytics.ts`（新增，若代码库无现成埋点SDK）`[ASSUME]`

### 你要做什么
- 后端：在登录成功、账号不存在、密码错误、账号锁定拒绝这四类结果分支各自触发一次埋点/日志事件，事件需包含结果类型（成功/账号不存在/密码错误/已锁定）及发生时间，具体投递方式（写日志表/调用现有埋点SDK/推送消息队列）按代码库现有基础设施选型，材料未指定具体工具，需在交付报告中说明选用方案。
- 不要求本任务实现运营看板本身，只要求产生可被看板消费的事件数据。

### 明确不要做的事
- 不要修改特性开关的开放范围/翻转逻辑
- 不要改动任务1~4已实现的登录判断逻辑本身，只在其结果分支上追加埋点调用
- 不要顺带实现运营看板前端页面（不在本故事范围内）

### 完成定义（DoD）
- `[FACT]` 对应AC-5：登录成功、账号不存在、密码错误、账号锁定四种场景分别有测试断言对应埋点/日志调用被触发一次，且事件中可区分结果类型（`[INFER]` "四类分支各打点"由AC-5"登录成功/失败需要埋点"结合任务2、任务3已产出的失败细分类型推导得出，AC-5原文未逐类列举，已同步收录进假设与待确认项）。

### 交付要求
改动应能独立形成一个PR，范围只覆盖本任务。完成后返回：实际改了哪些文件（与"预计涉及文件"逐项核对，一致的不用重复说明，不一致的必须说明原因）、新增了哪些测试用例、是否有偏离本任务范围的地方。

---

## 任务6

你要做的事：全量开启登录/工作台访问保护，并从代码库中彻底删除本故事引入的特性开关。
对应验收标准：不直接对应新增AC，是对AC-1~AC-5已实现能力的发布启用与技术债清理收尾动作，不新增用户可见行为，只做"从灰度到全量"的开关翻转与代码清理。

本任务信息自包含：不要假设除下文之外还有更多规划背景，下面就是执行这个任务所需的全部上下文（不管你是被派去执行它的subagent、另一个新对话，还是人工阅读）。

### 背景
这是"用户名密码登录"纵向安全切片计划中的第6/6个任务，也是最后一个任务。前置状态：任务1~5均已合并，登录功能已完整覆盖AC-1~AC-5全部行为，且在灰度/预发环境通过 `feature_login_auth_enabled` 开关验证通过（含AC-5埋点数据在运营看板口径正确）。本任务完成后，`feature_login_auth_enabled` 开关将从代码库中物理移除，登录与工作台访问保护成为系统默认行为，不再有开关判断分支。

### 预计涉及文件
未提供代码库路径，无法预估文件，由实现方在交付报告中列出实际改动文件 `[UNKNOWN]`。参考路径（需用任务1~5的实际交付结果核对开关判断分支具体分布在哪些文件后再执行删除，不要凭本清单假设遗漏或多删）：
- 任务1新增的特性开关配置文件，如 `src/config/featureFlags.ts`（删除该开关的定义与读取逻辑）`[INFER]`
- 各任务中出现开关判断的文件（登录路由、登录服务、前端路由守卫等，具体清单以任务1~5交付报告中提到的开关引用位置为准）`[UNKNOWN]`
- 工作台路由保护中间件/守卫，如后端 `src/middleware/requireAuth.ts`、前端路由守卫组件（新增或由条件判断改为无条件启用）`[ASSUME]`

### 你要做什么
- 后端：将工作台相关路由（或需要登录态保护的路由）的登录校验中间件从"仅开关开启时生效"改为默认常态生效；删除 `feature_login_auth_enabled` 开关的定义、读取、判断分支。
- 前端：未登录状态访问工作台路由时，默认重定向到登录页（不再依赖开关判断）；删除前端侧对该开关的判断逻辑。
- 删除后需确认代码库中不再存在对该开关名称的任何引用（配置项、环境变量声明、条件判断均需清理）。

### 明确不要做的事
- 不要在本任务中新增/修改AC-1~AC-5已实现的业务逻辑本身（如密码规则、锁定时长、会话时长），本任务只做开关翻转与清理
- 不要顺带清理与本故事无关的其他历史开关或技术债
- 不要跳过"确认无引用"这一步就直接删除开关文件

### 完成定义（DoD）
- 未登录用户直接访问工作台路由，无需任何开关配置即被重定向到登录页，行为与灰度环境开关开启时一致（`[INFER]` 由前置任务已验证的行为推导，全量开启不应改变已验证行为，只改变默认可见范围）。
- 代码库中检索开关名称 `feature_login_auth_enabled`（或实际使用的最终命名）应无任何残留引用（交付报告需说明用什么方式确认，如全局搜索结果）。
- AC-1~AC-5此前各任务的测试用例在去除开关后重新执行仍全部通过。

### 交付要求
改动应能独立形成一个PR，范围只覆盖本任务。完成后返回：实际改了哪些文件（与"预计涉及文件"逐项核对，一致的不用重复说明，不一致的必须说明原因）、新增了哪些测试用例、是否有偏离本任务范围的地方，以及开关清理的确认方式。

## 8. 任务执行方式说明

路线图（任务1~6）为一次性产出，但派发执行时必须按编号顺序逐个进行，不要把6份任务打包到同一次执行调用中。每派发下一个任务前，需用上一个任务实际交付报告中的"实际改了哪些文件""是否偏离预计范围"两项结果，核对下一个任务模板里的"前置状态"与"预计涉及文件"字段是否仍准确——尤其是任务2~6中标注 `[INFER]`（基于前序任务预计路径类推）的文件路径，一旦前序任务的实际落地路径与预计不符，必须先更新该字段再派发，不能沿用派发前写死的预判。已批准的PR应尽快合并后再派发下一个任务，不要让已批准的PR堆积等待。
