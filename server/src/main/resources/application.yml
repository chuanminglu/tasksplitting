server:
  port: 4000
spring:
  application:
    name: tasksplitting-api
  datasource:
    url: jdbc:sqlite:${DB_PATH:server/prisma/dev.db}
    driver-class-name: org.sqlite.JDBC
    hikari:
      maximum-pool-size: 5
  sql:
    init:
      mode: always
